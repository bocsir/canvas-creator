export interface CanvasInput {
    gridSpacing: number;
    lineWidth: number;
    lineLength: number;
    mouseEffect: string;
    mouseRadius: number;
    colorList: string[],
    animate: boolean,
    angleFunc: string,
    lineToXFunc: string, 
    lineToYFunc: string
}
